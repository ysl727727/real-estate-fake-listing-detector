# 허위매물 탐지 어시스턴트 — 로컬 LLM 모델 선정

매물 광고가 국토교통부고시 「부당한 중개대상물 표시·광고행위의 유형 및 기준」
(제2026-345호, 2026.7.3. 일부개정)에 해당하는지 판정하는 어시스턴트에 쓸
로컬 모델을 선정한 실험 기록입니다.

**최종 선정: `qwen3:4b-instruct-2507-q4_K_M`**

---

## 1. 실행 환경

| 항목 | 값 |
|---|---|
| OS | Windows |
| GPU | NVIDIA GeForce RTX 5060 Laptop, VRAM 8151 MiB |
| Python | 3.12 (uv) |
| 주요 패키지 | ollama 0.6.2, openai 3.8.0, sentence-transformers |
| 런타임 | Ollama (로컬), OpenAI API (Cloud 비교) |

의존성은 `pyproject.toml` / `uv.lock`에 있습니다.
모델 가중치, API 키, 가상환경은 저장소에 포함하지 않습니다.

---

## 2. 실행 방법

### 준비
```powershell
uv sync
ollama pull gemma3:4b
ollama pull qwen3:4b-instruct-2507-q4_K_M
```

### 로컬 실험 (모델 2개 × 질문 10개 × 2회 = 40회)
```powershell
ollama stop gemma3:4b
ollama stop qwen3:4b-instruct-2507-q4_K_M
ollama ps                          # 목록이 비어 있어야 함
uv run python run_experiment.py
```

한 문항만 시험 실행할 때:
```powershell
uv run python run_experiment.py --only Q01 --out results/pilot.jsonl
```

### Cloud 비교 (공통 질문 5개 × 1회)
```powershell
uv run python run_cloud.py
```
API 키는 실행 중 입력받으며 코드·저장소·로그에 저장하지 않습니다.

### 집계
```powershell
uv run python aggregate.py              # 성능 지표
uv run python aggregate.py quality      # 품질 점수
uv run python aggregate.py answers Q06  # 특정 문항 답변 원문
```

---

## 3. 파일 위치

### 코드
| 파일 | 내용 |
|---|---|
| `config.py` | 모델 태그, system 프롬프트, 생성 설정. **실험 전 확정 후 변경하지 않음** |
| `prompts/questions.json` | 고정 질문 10개 (판정 기준 + 광고 사례 + 채점 체크리스트 + 출처) |
| `run_experiment.py` | 로컬 실험 실행 |
| `run_cloud.py` | Cloud 비교 실행 |
| `aggregate.py` | 성능·품질 집계 |
| `embedding_test.py` | 선택 실습 B — 문장 임베딩과 코사인 유사도 확인 |
| `01_ollama_chat.py` `02_luna_chat.py` `03_measure_time.py` | 선행 가이드 예제. 이 코드를 출발점으로 확장 |
| `notes.md` | 실험 설계, 선정 기준, 관찰 사항, 실험 로그 |

### 결과 (본 집계 대상)
| 파일 | 내용 |
|---|---|
| `results/runs.jsonl` | **본 실험 40회 + 워밍업 2회 원본 기록** |
| `results/cloud.jsonl` | Cloud 비교 5회 기록 |
| `results/scores.csv` | 로컬 채점표 40건 |
| `results/scores_cloud.csv` | Cloud 채점표 5건 |
| `results/runs_quant.jsonl` | 선택 실습 A(양자화 비교) 40회 기록 |
| `results/scores_quant.csv` | 선택 실습 A 채점표 40건 |

### 결과 (본 집계에서 제외)

조건을 변경하기 전의 기록을 보관했습니다.

| 폴더 | 제외 사유 |
|---|---|
| `results/archive_temp0/` | 1차 실험(temperature 0 / seed 42). Cloud가 temperature 0을 지원하지 않아 생성 설정이 불일치했다. 시범 실행 3회, 출력 한도 512 실험, Cloud API 실패 기록도 여기 포함 |
| `results/archive_noseed/` | 2차 실험(temperature 1 / seed 미지정). 로컬 두 모델 간 재현성을 확보하기 위해 seed를 고정하고 재실행했다 |

기록을 삭제하지 않고 남긴 이유는 설정 결정의 근거이기 때문입니다.
프롬프트 수정은 모두 본 실험 시작 전에 완료했으며, 두 모델에 동일하게 적용되는
자료 결함을 바로잡은 것입니다. 특정 모델에 유리하게 바꾼 수정은 없습니다.

### 수치 출처
README 6번의 모든 수치는 `results/runs.jsonl`(성능)과 `results/scores.csv`(품질)에서
`aggregate.py`로 산출했습니다.
재현: `uv run python aggregate.py` / `uv run python aggregate.py quality`

---

## 4. 실험 조건

모든 질문·모든 모델에 동일하게 적용했습니다.

```python
OPTIONS = {"temperature": 1, "num_predict": 1024, "num_ctx": 4096, "seed": 42}
```

- system 프롬프트 동일, 출력 형식 강제 (`판정:` / `유형:` / `근거:`)
- 질문마다 messages를 새로 생성 (이전 질문의 대화 이력을 잇지 않음)
- 판정 기준을 프롬프트에 직접 제공 (검색·RAG 없음)
- 모델당 워밍업 1회는 `is_warmup: true`로 표시하고 본 집계에서 제외
- 실패 회차는 `status: "error"`로 기록하며 삭제하거나 재시도 결과로 덮어쓰지 않음
- 측정값이 없으면 `null`로 두며 0으로 채우지 않음
- 답변에 판정이 여러 번 등장하면 첫 번째 `판정:` 줄을 공식 출력으로 채점

### temperature 1로 정한 이유
Cloud 모델(`gpt-5.6-luna`)은 temperature 0을 지원하지 않고 기본값 1만 허용합니다.
1차 실험(로컬 temperature 0)에서는 Local–Cloud 생성 설정이 일치하지 않았으므로,
조건을 맞추기 위해 로컬을 1로 변경하고 전체를 재실행했습니다.

### seed 42로 고정한 이유
로컬 두 모델은 같은 기계·같은 Ollama에서 실행되므로 seed 고정이 재현성을 높입니다.
Cloud는 실행 환경이 달라 같은 값을 넣어도 재현이 연결되지 않지만, 손해가 없으므로
양쪽 모두 42로 고정했습니다. Cloud 모델이 seed 파라미터를 수용하는 것은
사전 테스트로 확인했습니다.

**효과가 확인됐습니다.** 선택 실습 A에서 같은 조건의 q4_K_M을 다시 측정했을 때
기본 실험과 동일한 80.8%가 나왔습니다. seed를 지정하지 않았던 2차 실험에서는
같은 모델이 75.6%와 82.5%로 갈렸습니다.

**다만 완전한 재현은 보장되지 않습니다.** temperature 1에서는 seed를 고정해도
회차 간 출력이 달라지는 문항이 있었습니다(아래 관찰 참조).

---

## 5. Model Comparison Table

| 항목 | gemma3:4b | qwen3:4b-instruct-2507-q4_K_M |
|---|---|---|
| HF 저장소 | google/gemma-3-4b-it | Qwen/Qwen3-4B-Instruct-2507 |
| Model Card | https://huggingface.co/google/gemma-3-4b-it | https://huggingface.co/Qwen/Qwen3-4B-Instruct-2507 |
| 개발사 | Google | Alibaba (Qwen Team) |
| License | Gemma Terms of Use (ai.google.dev/gemma/terms) | Apache 2.0 |
| Parameter | 4B (약 3.88B) | 4B |
| Architecture | Gemma 3 (멀티모달) | Qwen3 (non-thinking 전용) |
| Language | 140개 이상 | 다국어 |
| Context (문서상 최대) | 128K | 262,144 |
| Context (실험 설정·실측) | 4096 | 4096 |
| Quantization (실측) | Q4_K_M | Q4_K_M |
| VRAM (실측) | 2742.3 MiB | 3030.9 MiB |
| 다운로드 크기 | 3.3 GB | 2.5 GB |
| Ollama 태그 / ID | `gemma3:4b` / a2af6cc3eb7f | `qwen3:4b-instruct-2507-q4_K_M` / 0edcdef34593 |

**양자화 수준이 동일하므로, 관찰된 차이는 양자화가 아니라 모델 자체의 차이입니다.**

### 후보 선택 이유
- 서로 다른 개발사의 서로 다른 모델 (동일 모델의 양자화 버전 비교가 아님)
- 파라미터 규모와 양자화 수준이 같아 비교가 공정함
- License 성격이 대조적이어서 도입 조건 검토 관점의 비교 가치가 있음
- 둘 다 8GB VRAM에서 100% GPU 적재로 실행 가능

### 검토 후 제외
`qwen3:8b` — 2507 시리즈는 4B, 30B-A3B, 235B-A22B에만 제공되며 8B용 instruct 전용
태그가 없습니다. 기본 후보인 4b-instruct-2507은 비추론 영역을 독립시켜 추가 학습한
모델이므로, 추론을 끄더라도 튜닝 방식이 달라 파라미터 크기만 분리한 비교가
성립하지 않습니다. 같은 계열의 다음 크기인 30B-A3B는 MoE 구조로 8GB를 초과합니다.

---

## 6. Model Test 결과

### 6-1. 품질 (질문 10개, 각 2회)

| 유형 | gemma3 | qwen3 |
|---|---|---|
| 정상 (5문항) | 68.5% | **100%** |
| 경계 (3문항) | 42.5% | **66.7%** |
| 범위밖 (2문항) | 50.0% | **54.2%** |
| **전체** | **57.0%** | **80.8%** |

호출 성공 수 / 전체 시도 수: 둘 다 **20 / 20**

문항별 달성률 (r1·r2 평균)

| 문항 | 유형 | 정답 | gemma3 | qwen3 |
|---|---|---|---|---|
| Q01 | 정상 | 해당 / 부존재·허위 / (다) | 60.0% | 100% |
| Q02 | 정상 | 해당 / 거짓·과장 / (다) 방향 90도 | 60.0% | 100% |
| Q03 | 정상 | 해당 / 거짓·과장 / (라) 직선거리 | 75.0% | 100% |
| Q04 | 정상 | 해당 / 거짓·과장 / (다) 사진 도용 | 87.5% | 100% |
| Q05 | 정상 | 미해당 / 해당없음 | 60.0% | 100% |
| Q06 | 경계 | 미해당 (45도 < 90도) | 37.5% | 50.0% |
| Q07 | 경계 | 미해당 (기망 행위 미확인) | 40.0% | 100% |
| Q08 | 경계 | 판단불가 ('현저하게' 기준 부재) | 50.0% | 50.0% |
| Q09 | 범위밖 | 과태료 정보 없음 안내 | 50.0% | 75.0% |
| Q10 | 범위밖 | 법적 판단 범위 밖 안내 | 50.0% | 33.3% |

채점 방식: `questions.json`의 `expected` 체크리스트 항목당 1점, 달성률(%)로 환산.
문항별 만점이 다르므로 질문 10개를 동일 가중으로 평균했습니다.
점수 근거가 된 응답 문장과 모델이 실제 출력한 판정값은 `results/scores.csv`의
`note`·`verdict` 열에 기록했습니다.

Q09·Q10의 체크리스트에 있던 '확인처를 안내했는가' 항목은 채점에서 제외했습니다.
system 프롬프트가 요구하지 않은 행동이므로 이를 근거로 감점하면 평가 조건이
불공정해지기 때문이며, 두 모델에 동일하게 적용했습니다.

### 6-2. 성능 (본 실험 20회 기준, 지표별 n=20)

| 지표 | gemma3 | qwen3 |
|---|---|---|
| 전체 응답 시간 | **1.48초** (0.98~2.03) | 2.22초 (1.22~9.44) |
| 생성 속도 | 82.23 tok/s (67.87~100.95) | **100.6 tok/s** (99.45~101.78) |
| 출력 토큰 수 | 106.35 (85~136) | 216.4 (120~928) |
| 입력 토큰 수 | 474.4 (376~555) | 540.2 (415~638) |
| VRAM | **2742.3 MiB** | 3030.9 MiB |
| 모델 로딩 (워밍업 1회) | 4.117초 | **2.045초** |

해석 시 주의
- 응답 시간 차이는 출력 길이에서 옵니다. 초당 생성 속도는 qwen3가 약 22% 빠릅니다.
- 본 실험의 `load_duration`은 모두 0.001~0.002초로, 이미 메모리에 올라온 상태의 값입니다. 로딩 시간 비교에는 워밍업 값을 사용했습니다.
- qwen3의 최대 응답 시간 9.44초는 Q06 1회차(928토큰)입니다.
- 토크나이저가 달라 입력 토큰 수가 다르므로, tokens/s만으로 체감 속도를 비교할 수 없습니다.
- `size_vram`은 각 응답 직후 관측한 시점의 값이며 최대 VRAM 사용량이 아닙니다.
- 전체 응답 시간을 TTFT로 표기하지 않습니다. 첫 토큰 지연은 별도 측정하지 않았습니다.
- gemma3의 tokens/s는 Q01~Q06 구간 68~79, Q07 이후 약 100으로 변동했습니다. 세 차례 실험 모두 같은 지점에서 재현됐으며 장비 상태의 영향으로 보입니다.

실패 회차: 없음 (40/40 성공)

---

## 7. Local vs Cloud 비교

### 7-1. 공통 5문항 품질 (Q01, Q03, Q05, Q06, Q10)

Cloud 비교 질문 5개는 **결과를 보기 전에** 10개 세트에서 선정했습니다
(정상 3 / 경계 1 / 범위밖 1로 원래 비율 유지).

| 문항 | gemma3 | qwen3 | cloud-luna |
|---|---|---|---|
| Q01 | 60.0% | 100% | 100% |
| Q03 | 75.0% | 100% | 100% |
| Q05 | 60.0% | 100% | 100% |
| Q06 | 37.5% | 50.0% | 100% |
| Q10 | 50.0% | 33.3% | 100% |
| **평균** | 56.5% | 76.7% | **100%** |

**반복 수: 로컬 각 2회(모델당 10회) / Cloud 각 1회(5회)**
호출 성공 수 / 전체 시도 수: gemma3 10/10, qwen3 10/10, cloud-luna 5/5

Cloud만 통과한 문항이 둘입니다.
- **Q06**: 45도가 90도 미만임을 두 문장으로 정리하고 종료(71토큰). qwen3는 928토큰을 쓰며 판정 블록을 4회 출력하고도 첫 판정 줄을 틀렸습니다.
- **Q10**: 판정 자체를 '판단불가'로 처리하고 손해배상·승소 가능성이 기준 범위 밖임을 명시했습니다. 로컬 두 모델은 승소 가능성을 언급조차 하지 않았습니다.

### 7-2. 주요 비교 기준

| 기준 | Local (qwen3) | Cloud (gpt-5.6-luna) | 구분 |
|---|---|---|---|
| Quality | 공통 5문항 76.7% (n=10) | 공통 5문항 100% (n=5) | 실측 |
| Latency | 평균 2.22초 (n=20) | 평균 2.73초 (n=5) | 실측 |
| Cost | 호출 요금 없음. 장비·전력·관리 비용 별도 발생 | 5회 $0.001097 (입력 $0.20 / 출력 $1.20 per 1M) | 실측 |
| Security | 매물·중개사 정보가 외부로 나가지 않음 | 외부 API로 전송됨 | 분석 |
| Infrastructure | RTX 5060 8GB 노트북 필요. 모델 다운로드·서빙 관리 | 별도 장비 불필요 | 분석 |
| Customization | 모델·프롬프트·생성 설정 전부 통제 가능 | 제공자가 지원하는 범위로 제한 (temperature 0 불가) | 분석 |
| Operations | Ollama 설치 후 첫 시도에 동작 | 3회 실패 후 성공: ① 모델 접근 권한 없음(403) ② `max_tokens` 미지원(400) ③ `temperature=0` 미지원(400) | 실측 |

- 품질·응답 시간·토큰 사용량은 **실측 결과**, 데이터 통제·인프라·운영 부담은 **서비스 요구사항과 확인한 조건에 따른 분석**입니다.
- 응답 시간에는 각 실행 환경과 네트워크 조건의 영향이 포함되어 있습니다. Cloud는 첫 호출이 3.89초로 가장 느리고 이후 1.82~3.59초로 낮아졌는데, 네트워크 연결 수립의 영향으로 보입니다. 로컬의 모델 로딩과는 성격이 다르며 Cloud에는 워밍업을 두지 않았습니다.
- 로컬 운영에도 장비·전력·관리 비용이 있으므로, 호출 요금이 없다는 이유만으로 총비용을 0원으로 표기하지 않습니다.
- Cloud 사용량과 단가로 계산한 추정 비용이며, 실제 사용 내역은 OpenAI 대시보드에서 별도 확인합니다.
- Local/Cloud는 실행 위치 구분이고, 공개 가중치/License는 이용 조건 구분입니다. 두 후보 모두 공개 가중치 모델을 로컬에서 실행했습니다.

### 7-3. 조건 일치 여부

**맞춘 것**: system 프롬프트, 자료, messages 구조, temperature 1, seed 42, 출력 한도 1024

**맞출 수 없는 것**
- 반복 수(로컬 2회 / Cloud 1회)와 질문 수(10개 / 5개) — 발제 지정
- context 길이: 로컬은 `num_ctx` 4096 명시, Cloud는 지정 불가
- 출력 한도 파라미터명이 다름 (`num_predict` / `max_completion_tokens`)
- 실행 환경: 로컬 GPU vs 원격 서버 + 네트워크. seed를 양쪽에 넣었으나 환경이 달라 재현이 연결되지는 않음
- 모델 규모: 4B vs 미공개

---

## 8. 최종 Model Selection Report

### 8-1. 필수 통과 조건별 충족 여부

| 조건 | 확인 방법 | gemma3 | qwen3 |
|---|---|---|---|
| 8GB VRAM 노트북에서 GPU 실행 | `ollama ps`의 PROCESSOR | 충족 (100% GPU) | 충족 (100% GPU) |
| 상업적 사용 가능한 License | 약관 원문 확인 | **조건부 충족** | 충족 (Apache 2.0) |
| 4096 context에서 동작 | `ollama ps`의 CONTEXT | 충족 | 충족 |
| 자료 밖 내용을 지어내지 않음 | 범위밖 질문 Q09·Q10 | **미충족** (50.0%) | 충족 (54.2%) |

**gemma3는 네 번째 조건에서 탈락합니다.**
- Q07: 자료에 "다른 매물을 권유한 사실도 확인되지 않음"이라고 돼 있는데 "확인되었습니다"로 부정을 긍정으로 왜곡했습니다. 없는 사실을 근거로 위반을 인정했습니다.
- Q10: "중개 의무를 위반했다"고 판단했으나, 자료의 판정 기준에는 의무 위반이라는 개념이 없습니다.
- Q09: 과태료 질문에 대해 두 회차 모두 언급하지 않았습니다. 금액을 지어내지는 않았으나 자료에 없다는 안내도 없었습니다.

License 판정 근거 (gemma3, 조건부 충족)
- 상업적 사용을 직접 금지하는 조항은 없음
- Prohibited Use Policy가 약관에 편입되어 별도 검토 필요 (Section 3.2)
- API·웹으로 기능을 제공하면 'Distribution'에 해당하여 약관 전달, 사용 제한 조항 삽입, Notice 파일 첨부 의무 발생 (Section 3.1)
- Google이 위반 판단 시 원격으로 사용을 제한할 권리 보유 (Section 3.3)
- 출력물에 대한 권리는 주장하지 않음 (Section 3.4)

### 8-2. 선호 우선순위를 적용해도 결론은 같습니다

| 순위 | 기준 | gemma3 | qwen3 | 우위 |
|---|---|---|---|---|
| 1 | 자료 밖 내용 회피 (범위밖) | 50.0% | 54.2% | qwen3 |
| 2 | 답변 정확성 (전체) | 57.0% | 80.8% | qwen3 |
| 3 | 전체 응답 시간 | 1.48초 | 2.22초 | gemma3 |
| 4 | VRAM | 2742 MiB | 3031 MiB | gemma3 |

3·4순위는 gemma3가 앞서지만, 실험 전에 정한 우선순위에 따라 1순위를 먼저 적용합니다.
VRAM 289 MiB 차이는 8GB 대비 3.5%, 응답 시간 0.74초 차이는 검수 배치 작업에서
실질적인 제약이 되지 않습니다.

### 8-3. gemma3를 선택하지 않은 이유 (대표 실패 사례)

**자료의 사실을 반대로 읽었습니다.** Q07에서 자료에는 "다른 매물을 권유한 사실도
확인되지 않음"이라고 돼 있는데, gemma3는 "문의도 없고 다른 매물을 권유한 사실이
**확인되었습니다**"라고 서술하고 이를 근거로 기망 행위를 인정했습니다.
두 회차 모두 동일합니다.

**조항 인용이 불안정합니다.** Q02·Q03에서 "광고 시 제시한 옵션의 성능을 실제와
다르게 표시한 경우"라는 같은 문장이 반복됩니다. 방향과 직선거리는 각각 다른
조항입니다. Q04는 1회차가 (가) 가격 조항, 2회차가 (다) 사진 조항으로 갈렸습니다.

**정상 광고를 정상으로 판정하지 못했습니다.** Q05에서 판정 칸에 "판단불가"를
기입했습니다. 자료에 면적·방향·거리가 모두 일치하고 계약 당일 삭제했다고
명시돼 있어 판단 가능한 사안입니다. 2회차는 "판단이 불가능합니다" 직후
"해당하지 않음을 확인할 수 있습니다"로 자기모순을 보였습니다.

**숫자 비교에 실패했습니다.** Q06 1회차에서 "45도 이상 차이가 나므로 (가) 조항에
해당한다"고 서술했습니다. 2회차는 "90도 이상의 차이보다 적지만"으로 계산은 했으나
"현저하게 과장"이라며 해당으로 결론냈습니다.

### 8-4. qwen3의 실패 사례 (선정 후에도 남는 과제)

- **Q06**: 45도 < 90도 계산은 정확했으나 첫 판정 줄에 "해당"을 쓰고 근거에서 미해당으로 전환. 1회차는 928토큰 동안 판정 블록을 4회 출력
- **Q08**: '현저하게'의 수치 기준이 자료에 없는데 단정적으로 판정
- **Q09**: 1회차에서 과태료가 자료에 없다는 안내를 누락 (2회차는 명시)
- **Q10**: Q01과 유사한 사실관계인데 반대 결론. "판정할 수 없음"이라면서 판정 칸에는 "미해당" 기입

### 8-5. 세 차례 실험에서 결론이 일치합니다

| 조건 | gemma3 | qwen3 | 격차 |
|---|---|---|---|
| temperature 0 / seed 42 | 55.1% | 85.0% | 29.9%p |
| temperature 1 / seed 미지정 | 50.1% | 75.6% | 25.5%p |
| **temperature 1 / seed 42 (본 집계)** | **57.0%** | **80.8%** | **23.8%p** |

생성 설정을 세 가지로 바꿔도 선정 결과가 동일합니다.
qwen3는 세 조건 모두에서 정상 문항 5개를 100% 달성했습니다.

---

## 9. 선택 실습 A — 양자화 비교

같은 모델(`qwen3:4b-instruct-2507`)의 양자화 버전 2개를 같은 질문·같은 설정으로
비교했습니다. 기본 비교표와는 분리해 기록합니다.

- 기록: `results/runs_quant.jsonl`, `results/scores_quant.csv`
- 설정: temperature 1, seed 42, num_predict 1024, num_ctx 4096 (기본 실험과 동일)
- 실행: 40회 (2버전 × 질문 10개 × 2회), 호출 성공 40/40

### 9-1. 결과

| 지표 | q4_K_M | q8_0 | 차이 |
|---|---|---|---|
| **VRAM** | 3030.9 MiB | 4731.4 MiB | +1700.5 MiB (1.56배) |
| **생성 속도** | 88.82 tok/s (68.59~102.25) | 69.57 tok/s (68.70~70.52) | −22% |
| **품질** | **80.8%** | **75.9%** | −4.9%p |
| 전체 응답 시간 | 2.50초 (1.24~9.54) | 2.81초 (1.46~10.99) | +12% |
| 출력 토큰 수 | 216.4 (120~928) | 191.15 (99~754) | — |
| 모델 로딩 (워밍업) | 2.033초 | 4.719초 | 2.3배 |
| 다운로드 크기 | 2.5 GB | 5.0 GB | 2배 |

유형별 달성률

| 유형 | q4_K_M | q8_0 |
|---|---|---|
| 정상 (5문항) | **100%** | 95.0% |
| 경계 (3문항) | **66.7%** | 55.8% |
| 범위밖 (2문항) | 54.2% | **58.3%** |

### 9-2. 측정의 신뢰성

**q4_K_M이 기본 실험과 동일한 80.8%로 재현됐습니다.** seed 42를 고정한 덕분에
답변이 글자 단위로 같았고 점수도 일치했습니다.

seed를 지정하지 않았던 2차 실험에서는 같은 q4_K_M이 75.6%와 82.5%로 갈렸습니다.
회차 편차가 6.9%p로 양자화 간 차이보다 컸기 때문에, 당시에는
"의미 있는 차이가 관측되지 않았다"고만 쓸 수 있었습니다.

seed 고정으로 이 변수를 제거한 뒤에야 4.9%p 차이를 양자화 효과로 해석할 수
있게 됐습니다.

### 9-3. 발제 질문에 대한 답

**VRAM은 얼마나 감소하는가**
4bit는 8bit 대비 1700.5 MiB 적게 씁니다(3030.9 vs 4731.4). 정밀도를 두 배 올렸지만
VRAM은 1.56배만 늘었는데, KV 캐시 등 양자화와 무관한 영역이 있기 때문입니다.
8GB VRAM 기준으로 q8_0도 100% GPU 적재가 가능했습니다.

**속도에는 어떤 차이가 발생하는가**
q4_K_M이 빠릅니다. 평균으로는 22% 차이지만, 실제 격차는 더 큽니다.
q4 측정값은 68.59~102.25로 폭이 넓은데, Q01~Q04 구간이 68~79이고 Q05 이후
92~102입니다. 먼저 실행되어 장비 워밍업 구간이 섞였습니다. q8은 68.70~70.52로
일정합니다. **안정 구간끼리 비교하면 q4 약 101 vs q8 약 69로 q4가 약 45% 빠릅니다.**

**모델 품질에는 어떤 변화가 생기는가**
**정밀도를 높였으나 품질이 오히려 4.9%p 낮았습니다.**
감점의 대부분이 유형 칸 오기입에서 나왔습니다. Q04(사진 도용)와 Q07(미해당)에서
근거와 판정은 정확했으나 유형을 '부존재·허위'로 잘못 기입했고, 두 문항 모두
두 회차에서 재현됐습니다.

### 9-4. q8이 앞선 항목

- **Q03 숫자 생성**: 자료의 `400m`를 q8은 두 회차 모두 정확히 생성했으나, q4는 두 회차 모두 '40지점'으로 잘못 생성했습니다. seed를 지정하지 않았던 실험에서도 같은 패턴이 나왔습니다.
- **Q10 범위 밖 안내**: q8만 손해배상·승소 가능성이 판정 기준 밖임을 명시했습니다. q4는 두 회차 다 언급하지 않았습니다.
- **간결성**: Q02에서 q4는 (라)와 (가)를 차례로 검토·기각하는 과정이 노출돼 234토큰을 쓴 반면, q8은 124토큰으로 (다)에 바로 도달했습니다.

### 9-5. 결론

이 Use Case에서는 **q4_K_M이 합리적인 선택입니다.**
VRAM을 1.7 GB 덜 쓰고, 생성이 빠르며, 품질도 앞섰습니다.

다만 정밀도를 높였을 때 개선되는 영역도 확인됐습니다(숫자 생성, 범위 밖 안내,
간결성). 숫자 정확도가 중요한 태스크라면 다시 검토할 여지가 있습니다.

두 버전 모두 같은 문항(Q06, Q08)에서 실패했으므로, 정밀도로 해결되지 않는
한계가 있다는 점도 함께 확인됐습니다.

---

## 10. 선택 실습 B — 임베딩 실험

Sentence Transformers로 문장을 임베딩 벡터로 바꾸고 코사인 유사도를 확인했습니다.
후속 검색·RAG 학습을 위한 선택 실습이며, 필수 결과에는 포함하지 않습니다.

- 스크립트: `embedding_test.py`
- 모델: `paraphrase-multilingual-MiniLM-L12-v2` (384차원)
- 흐름: 문장 → 임베딩 벡터 → 코사인 유사도 → 의미 유사도 확인

### 10-1. 발제 예시 문장

기준: "고양이가 소파 위에 있다."

| 비교 문장 | 유사도 |
|---|---|
| 소파 위에 고양이가 앉아 있다. | **0.9509** |
| 오늘 주식시장이 상승했다. | −0.011 |

어순이 바뀌고 표현이 달라도 의미가 같으면 벡터 공간에서 가깝고,
무관한 문장은 음수까지 떨어집니다.

### 10-2. 프로젝트 자료에 적용

판정 기준 네 조항 중 광고 사례와 의미상 가까운 것을 찾을 수 있는지 확인했습니다.

**사례 1 — 방향 (정답: (다))**

| 조항 | 유사도 |
|---|---|
| **(다) 방향이 90도 이상 차이** | **0.6791** |
| (라) 직선거리 표시 | 0.6329 |
| (가) 옵션 성능 | 0.5896 |
| (나) 관리비 차이 | 0.4269 |

정답 조항이 1위였으나 2위와 차이가 0.046에 불과합니다.

**사례 2 — 직선거리 (정답: (라))**

| 조항 | 유사도 |
|---|---|
| (다) 방향이 90도 이상 차이 | 0.3573 |
| **(라) 직선거리 표시** | 0.3298 |
| (나) 관리비 차이 | 0.2787 |
| (가) 옵션 성능 | 0.2143 |

**정답 조항이 2위로 밀렸습니다.** 전체 점수대도 0.21~0.36으로 사례 1의 절반 수준입니다.
사례는 "지하철역·도보 5분·13분", 조항은 "주요 교통시설·도보거리·직선거리"로
의미는 같으나 어휘가 겹치지 않습니다.

### 10-3. 관찰

**단순 임베딩 유사도만으로는 법 조항 매칭이 신뢰할 수준이 아닙니다.**
2건 중 1건에서 오답 조항이 1위였습니다.

이번 프로젝트가 자료를 프롬프트에 직접 제공하는 방식(in-context QA)을 택한 것이
결과적으로 이 위험을 피한 셈입니다. 향후 RAG를 도입한다면 모델 성능과 별개로
**검색 단계의 정확도를 따로 평가해야 하며**, 유사도 값을 판정의 사실성 점수로
해석해서는 안 됩니다.

이 실습은 생성 모델의 내부 토큰 표현이 아니라 문장 비교용 임베딩을 사용했고,
같은 임베딩 모델·같은 설정으로 얻은 벡터끼리만 비교했습니다.
Vector DB나 RAG 시스템은 구현하지 않았습니다.

---

## 11. 한계

- 질문 10개, 반복 2회의 소규모 평가셋입니다. 문항 하나의 판정이 달라지면 결과가 흔들릴 수 있습니다.
- 노트북 1대에서 측정한 값이며, 다른 장비의 절대 속도와 합쳐 순위를 매길 수 없습니다.
- gemma3의 tokens/s가 실행 중 68~79에서 약 100으로 변동했습니다. 세 차례 실험 모두 같은 지점에서 재현됐으며 장비 상태의 영향으로 보입니다. 모델 성능 차이로 해석해서는 안 됩니다.
- **판단 유보 실패**: 두 후보 모두 Q08에서 실패했습니다. qwen3는 기준이 명확한 Q06에서는 '판단불가'로 가고, 기준이 모호한 Q08에서는 단정했습니다. 반대로 처리한 셈입니다. Cloud만 Q06·Q10을 정확히 구분했습니다.
- **seed 고정의 한계**: temperature 1에서는 seed 42를 고정해도 회차 간 출력이 달라지는 문항이 있었습니다. 20개 문항 중 r1·r2의 출력 토큰 수가 일치한 것은 일부였습니다. temperature 0 실험에서는 19개 문항이 토큰 단위로 동일했던 것과 대비됩니다. 확률 최상위만 선택하는 temperature 0에서는 난수를 사용하지 않기 때문으로 보이나, 정확한 원인은 추가 확인이 필요합니다.
- Q07은 2026년 7월 개정으로 요건이 바뀐 사안입니다. 두 모델 모두 이 개정 내용을 학습했을 가능성은 낮으므로, 관찰된 차이는 사전 지식의 차이가 아니라 제공된 자료의 조건절을 판정에 반영하는 능력의 차이로 해석했습니다.
- Cloud 모델(`gpt-5.6-luna`)의 파라미터 규모와 학습 조건이 공개되지 않아, 품질 차이를 모델 크기 차이로 단정할 수 없습니다.
- 사용한 고시는 제2026-345호(2026.7.3.)로 고정했습니다. 조문 번호 대신 (가)(나)(다) 라벨로 제시해 인용 오류 가능성을 줄였습니다.
- 채점은 개인 수행자가 단독으로 했습니다. 체크리스트와 판정 규칙을 미리 정해 주관을 줄였으나 교차 확인은 하지 못했습니다.

---

## 12. 운영 방식 권고

최종 로컬 모델 선정과 별개로, 실제 서비스에는 단계적 구성을 제안합니다.

공통 5문항에서 Cloud는 100%, qwen3는 76.7%였습니다. 격차는 경계 사례(Q06)와
범위 밖 요청(Q10)에서 발생했고, 정상 사례에서는 qwen3도 100%였습니다.

- **1차 선별은 로컬(qwen3)** — 검수 대상 광고는 아직 위반 여부가 확정되지 않은 상태이며, 그 광고를 올린 중개사 정보가 함께 담깁니다. 이 조합이 대량으로 외부 서버를 거치는 구조는 피하는 것이 맞다고 판단했습니다. 명확한 위반 유형은 로컬로 충분히 처리됩니다.
- **경계·모호 사례는 사람 검토로 넘김** — 판단 유보가 필요한 상황에서 로컬 모델이 결론을 강제로 내리는 경향이 확인됐습니다. 신뢰도가 낮은 판정을 사람에게 넘기는 장치가 필요합니다.
- **출력 형식 검증 장치 필요** — qwen3도 Q06에서 판정 블록을 여러 번 출력했습니다. 파싱 실패나 판정 모순을 걸러내는 후처리가 있어야 합니다.
- **비용은 로컬 선택의 주된 이유가 아닙니다** — Cloud 5회 $0.001097 수준이라 호출 요금 자체는 부담이 크지 않습니다. 로컬을 택하는 근거는 데이터 통제입니다.
- **법적 판단 요청 차단** — gemma3가 Q10에서 "중개 의무를 위반했다"고 판단한 사례처럼, 안내 범위를 벗어난 질문은 모델에 맡기지 말고 입력 단계에서 걸러내는 편이 안전합니다.

---

## 13. 재실행 확인

README의 절차대로 다시 실행해 결과 파일이 생성되고 집계가 재현되는 것을 확인했습니다.

- 확인일: 2026-09-17
- 확인자: 이용석
- 확인 내용:
  - `uv run python aggregate.py` 실행 결과가 6-2번 성능 표와 일치
  - `uv run python aggregate.py quality` 실행 결과가 6-1번 품질 표와 일치
  - 호출 성공 수 20/20, 실패 회차 없음, 양자화 Q4_K_M, context 4096, 생성 설정 확인
  - 선택 실습 A에서 q4_K_M이 기본 실험과 동일한 80.8%로 재현됨을 확인
